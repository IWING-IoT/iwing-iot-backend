// const express = require("express");

// const authController = require("../controllers/authController");
// const categoryController = require("../controllers/categoryControllerOld");

// const router = express.Router();

// router.put("/:categoryId", authController.protect, categoryController.edited);

// router.get("/:categoryId", authController.protect, categoryController.getEntry);

// router.post(
//   "/:categoryId/entry",
//   authController.protect,
//   categoryController.createEntry
// );

// module.exports = router;
